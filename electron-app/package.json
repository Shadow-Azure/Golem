{
  "name": "golem-electron",
  "version": "0.1.0",
  "private": true,
  "main": "src/main.ts",
  "scripts": {
    "dev": "echo 'TODO: setup dev server'",
    "build": "echo 'TODO: setup build'",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/",
    "format:check": "prettier --check src/",
    "format": "prettier --write src/"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "prettier": "^3.4.0",
    "eslint": "^9.0.0"
  }
}
